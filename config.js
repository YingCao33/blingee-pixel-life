window.BLINGEE_PIXEL_CONFIG = {
  // Leave empty for local play or same-origin backend.
  // If the frontend is hosted on Vercel and the multiplayer server is hosted
  // elsewhere, set this to a WebSocket URL such as:
  // "wss://your-backend.example.com"
  wsUrl: ""
};
